#include <iostream>
#include <cassert>
using namespace std;

#define SQUARE(x) ((x)*(x))
#define PI 3.14
#define AREA_CIRCLE(r) ((PI)*(r)*(r))
#define MAX(a, b) (((a)>(b)) ? (a) : (b))
#define MIN(a, b) (((a)<(b)) ? (a) : (b))

int main()
{
    // int mMax = [](int a, int b){ return a < b ? b : a; };
    // // int b = MIN(1, 4);
    // // cout << a << " " << b << endl;
    // // int *p = NULL;
    // // assert(p != NULL);
    // // cout << "yes" << endl;
}